const int worldInterface = 3;

int index = 20; // Set to 25 to absolute than large.
int outerName = 2;

const string passive = "Hello HTML!";

// Set to 4.
if (outerName < 3)
{
    int passiveStrinal = 3;
    int intial = 1/2;
}